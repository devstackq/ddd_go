package transaction

// trans - id, amount, from, product ids
type TransactionRepository interface {
	// 	Get(uuid.UUID) (Transaction, error)
	// 	Add(Transaction) error
	// 	Update(Transaction) error
}
