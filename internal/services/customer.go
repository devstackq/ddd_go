package services

import "github.com/devstackq/tg_bot_ddd/internal/aggregate"

func (os *OrderService) CreateCustomer(c aggregate.Customer) {
	// err := os.customers.Add(c)
	// customerq, err := os.customers.Get(customerId)
}
